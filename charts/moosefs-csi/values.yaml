# Default values for moosefs-csi.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

master:
  host: "mfsmaster"
  port: "9421"

csi:
  root: "/"
  working: "/pv_data"
  logging: "true"

dns: "ClusterFirstWithHostNet"

storageClass:
  name: "moosefs-storage"
  default: "true"
